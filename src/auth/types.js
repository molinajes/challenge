const prefix = 'auth';

export const AUTHENTICATION_SUCCEEDED = `${prefix}/AUTHENTICATION_SUCCEEDED`;
export const AUTHENTICATION_FAILED = `${prefix}/AUTHENTICATION_FAILED`;
export const AUTHENTICATE = `${prefix}/AUTHENTICATE`;
export const REQUEST_RESET_CODE = `${prefix}/REQUEST_RESET_CODE`;
export const RESET_CODE_SENT = `${prefix}/RESET_CODE_SENT`;
export const RESET_PASSWORD = `${prefix}/RESET_PASSWORD`;

export const LOGOUT = `${prefix}/LOGOUT`;
export const LOGOUT_PENDING = `${prefix}/LOGOUT_PENDING`;
export const LOGOUT_SUCCEEDED = `${prefix}/LOGOUT_SUCCEEDED`;
export const LOGOUT_FAILED = `${prefix}/LOGOUT_FAILED`;

export const SET_REDIRECT = `${prefix}/SET_REDIRECT`;
